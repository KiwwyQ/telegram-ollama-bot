python-telegram-bot>=21.0,<22
httpx>=0.27.0
aiohttp>=3.9.0

# Remote database drivers (SQLite is intentionally NOT used on Render).
# SQLAlchemy async core supports both MySQL and PostgreSQL.
sqlalchemy[asyncio]>=2.0
aiomysql>=0.2.0
asyncpg>=0.29.0
